
public class NumOculto {
	private Integer numOculto;
	private Boolean acertado = false;
	
	public NumOculto() {
		this.numOculto = (int) (Math.random()*99+1);
	}
	
	synchronized public int adviniarNumero(int numero) {
		int resultado = 0;
		if(acertado != true) {
			if(numero == this.numOculto) {
				resultado = 1;
				acertado = true;
			}
		}else {
			resultado = -1;
		}
		return resultado;
	}

	public Integer getNumOculto() {
		return numOculto;
	}
	
	
}
